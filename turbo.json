{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build",
        "generate"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "generate": {
      "outputs": [
        "src/client/**",
        "openapi.camel.yaml"
      ]
    },
    "lint": {},
    "test": {},
    "clean": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
